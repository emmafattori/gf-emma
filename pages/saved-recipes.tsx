import { Header } from "@/components/header/header"
import "../globals.css"
export default function SavedRecipes() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <Header />
      This is the saved recipes pages
    </main>
  )
}
